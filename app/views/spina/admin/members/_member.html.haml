%li.dd-item
  %div.dd-item-inner
    = link_to spina.edit_admin_member_path(member), class: 'button button-link button-small sortable-link' do
      = member.full_name
    %span.pull-right
      = link_to spina.edit_admin_member_path(member), class: 'button button-small button-link dd-nodrag' do
        = icon('pencil-outline')
        = t('spina.edit')
